server:
  servlet:
    context-path: /springboot01

  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: user_mktop_mgm
    password: sFTULNax9mTM_Clk
    url: jdbc:mysql://fat-10.mysql.ppdaidb.com:6407/ppdai_mktop_mgm?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT&autoReconnect=true&failOverReadOnly=false
    type: com.alibaba.druid.pool.DruidDataSource
  druid:
    initial-size: 5
    min-idle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    filters: stat,wall,slf4j
    connectionProperties: druid.stat.mergeSql\=true;druid.stat.slowSqlMillis\=5000
    web-stat-filter:
      enabled: true
      url-pattern: "/*"
      exclusions: "*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/druid/*"
    stat-view-servlet:
      url-pattern: "/druid/*"
      allow: 127.0.0.1,192.168.8.109
      deny: 192.168.1.188
      reset-enable: false
      login-username: admin
      login-password: 123456
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 1GB

mybatis:
  mapper-locations: classpath:/mybatis/*.xml
  type-aliases-package: com.spbt.model
  configuration:
    map-underscore-to-camel-case: true


aaa:
  b: x

mybatis-plus:
  mapper-locations: classpath:/mybatisplus/*.xml
  type-aliases-package: com.spbt.model


